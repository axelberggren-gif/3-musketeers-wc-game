> **Canon** — current source of truth for this directory. If reality and this file disagree, fix this file in the same PR.

# lib/supabase/ — Supabase clients + generated types

## Purpose
Three flavours of Supabase client (browser, server-with-cookies, service-role) plus the
Next.js middleware session refresher and the generated DB type definitions. Everything
that touches Supabase imports from here.

## Key files
- `client.ts` — `supabaseBrowser()`: anon-key client for client components.
- `server.ts` — `supabaseServer()`: anon-key client wired to Next.js cookies (RLS-aware,
  acts as the logged-in user). Also `supabaseService()`: service-role client (bypasses
  RLS, for cron and admin work only).
- `middleware.ts` — `updateSession()`: refreshes the Supabase session on every request,
  invoked from the root middleware.
- `types.ts` — generated by `supabase gen types typescript` (run `npm run db:types`).

## Conventions
- **Choose the right client**:
  - Client component / browser code → `supabaseBrowser()`.
  - Server component / server action / route handler acting on behalf of a user →
    `supabaseServer()`.
  - Cron / admin / migration backfills that legitimately need RLS-bypass →
    `supabaseService()`.
- Type-safe queries: `supabase.from("table").select(...)` infers from `types.ts`. If
  the DB schema changes, run `npm run db:types` to regenerate.

## Invariants (do not break)
- **Never call `supabaseService()` from user-facing code** without explicit
  authorization checks first. It bypasses RLS — leaks are catastrophic for a
  predictions game (you'd reveal locked picks).
- `supabaseServer()` is `async` (returns a Promise) because Next.js 16 `cookies()` is
  async. Always `await` it.
- When the DB schema changes (new migration), regenerate `types.ts` via
  `npm run db:types`. Commit the regenerated file in the same PR.

## Known gotchas
- The `cookies.setAll()` callback in `supabaseServer()` is intentionally wrapped in
  try/catch — Server Components cannot mutate cookies; ignoring the error there is
  correct (the middleware does the actual writing).
- Environment variables required: `NEXT_PUBLIC_SUPABASE_URL`,
  `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-only).
- Embedded relations on `matches` (which has two FKs to `teams` — `home_team_id` +
  `away_team_id`) MUST use the column-name hint syntax `teams!home_team_id(...)`
  / `teams!away_team_id(...)`. The legacy `home:home_team_id(...)` form is
  PostgREST-valid but supabase-js's typed select parser rejects it with
  `SelectQueryError<"Could not embed because more than one relationship was
  found...">`. Nested relations through `match_predictions.match_id → matches`
  also need the hint (`match:matches!match_id(...)`).
- `LeagueStandingsRow` is the **non-null** business shape; `Tables<"league_standings">`
  from the generated types declares every column nullable (because the view uses
  `coalesce(sum(...), 0)::int`, which the CLI can't introspect). Cast at the
  boundary: `(rows ?? []) as LeagueStandingsRow[]`. Same pattern in the realtime
  refetch inside `LeaderboardLive`.

## Recent changes
<!-- Newest first. Keep last 10. One line per entry. -->
- 2026-06-08: `types.ts` hand-edited to mirror migration `0022_manual_admin_props.sql` (dev DB hasn't applied it yet — `npm run db:types` reproduces the same once it has): `tournament_predictions` Row/Insert/Update gained `neymar_minutes_pick` / `streaker_pick` (`boolean | null`), `best_goalkeeper_player_id` / `golden_boot_team_id` / `war_game_match_id` (`string | null`) and `own_goals_guess` / `swedish_players_guess` (`number | null`); a new `manual_prop_resolutions` table type (with FK relationships to teams / players / matches); and the `score_manual_props` RPC in `Functions`. Needed so the new `/predict/outcomes` picks, the `/admin/props` resolver, and the `setManualPropResolutions` action typecheck against the strict `<Database>` generic.
- 2026-06-08: `types.ts` hand-edited to drop the `group_winner_predictions` + `group_settlements` table types and the `score_group_winner` RPC, mirroring migration `0021_remove_group_winner_prop.sql` (group-winner prop removed). Hand-edited because the dev DB hasn't applied 0021 yet; `npm run db:types` reproduces the same once it has.
- 2026-06-08: `types.ts` `tournament_predictions` Row/Insert/Update gained `final_goals_guess` / `biggest_win_margin_guess` / `golden_boot_goals_guess` / `total_red_cards_guess` (all `number | null`), hand-edited to mirror migration `0020_more_outright_props.sql` (the dev DB hasn't applied it yet — `npm run db:types` reproduces the same once it has). Lets the new `/predict/outcomes` page read the strongly-typed Row directly (no `Record<string, unknown>` cast) and the new `setTournamentPick` fields typecheck against the strict `<Database>` generic.
- 2026-06-04: `types.ts` `profiles` Row/Insert/Update gained `onboarded: boolean` (hand-edited to mirror migration `0015_profile_onboarding.sql`, which the dev DB hasn't applied yet — `npm run db:types` will produce the same once it has). Needed so the new `completeOnboarding` action's `.update({ onboarded: true })` typechecks against the strict `<Database>` generic.
- 2026-05-26: `types.ts` `Enums.stage` (and the matching `Constants.stage` array) extended with `"R32"` between `"GROUP"` and `"R16"` to mirror the position the migration sets via `ALTER TYPE stage ADD VALUE 'R32' BEFORE 'R16'`. Hand-edited rather than CLI-regenerated because migration `0013_add_r32_stage.sql` hasn't been applied to the dev DB yet — once it is, `npm run db:types` will produce the same shape. Without this, the strict `<Database>` typing introduced by the previous regen rejected `syncFixtures()`' `stage: "R32"` upsert.
- 2026-05-26: `types.ts` regenerated in supabase-CLI format — full `Database` namespace covering all 23 tables + `league_standings` view + 18 RPC functions + 6 enums, with `Tables<>` / `TablesInsert<>` / `TablesUpdate<>` / `Enums<>` helper aliases. Legacy interface names (`Pick1X2`, `Tournament`, `BanterMessage`, `BanterReply`, `LeagueStandingsRow`) kept as type aliases at the bottom for backward compat. `<Database>` now threaded through `supabaseBrowser()`, `supabaseServer()`, `supabaseService()`, and `updateSession()` so queries are fully type-safe. Required bumping `@supabase/ssr` from `^0.5.1` to `^0.10.3` — earlier versions used a 3-generic `SupabaseClient` signature incompatible with `@supabase/supabase-js@2.106`'s 5-generic one (Schema fell back to `never`, killing all query inference). `unwrapRelation()` helper deleted from `lib/utils.ts` and dropped from all 10 call sites — embedded single-FK relations are now correctly typed as `T | null` via the new generic. Closes #14.
