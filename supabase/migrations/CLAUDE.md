> **Canon** — current source of truth for this directory. If reality and this file disagree, fix this file in the same PR.

# supabase/migrations/ — Postgres schema, RLS, scoring, cron

## Purpose
SQL migrations applied in lexical order to the Supabase project. Defines tables, RLS
policies, lock triggers, scoring functions, the leaderboard view, and the pg_cron job
schedule that polls football-data.org.

## Key files
- `0001_init.sql` — Tables, enums, RLS policies, lock triggers. The foundation.
- `0002_scoring.sql` — `points_*` constant functions, `score_match`, `score_bracket`,
  `score_tournament_props`, `score_player_props`, `refresh_league_standings` view.
- `0003_cron.sql` — `pg_cron` + `pg_net` setup, `call_cron_endpoint()` helper,
  `sync_fixtures` (every 10 min) and `sync_scorers` (daily 06:00 UTC) schedules.

## Conventions
- File naming: `NNNN_<short_name>.sql`, four-digit zero-padded sequence. Next free
  number is whatever follows the highest existing file.
- Functions are `create or replace` so re-applying a migration is safe. Tables use
  `create table if not exists`; enums use `do $$ ... exception when duplicate_object
  then null; end $$;`.
- Scoring functions are `security definer` with `set search_path = public` so they
  can be called from RLS contexts.

## Invariants (do not break)
- **Migrations are append-only**. Never edit a file that's been merged to `main`.
  Add a new numbered file to make changes.
- **Points sync** (mirrored from `lib/scoring/CLAUDE.md`): the `points_*` SQL
  functions in `0002_scoring.sql` MUST equal the `POINTS` object in
  `lib/scoring/rules.ts`. Any change is a two-file edit (a new migration here + the
  TS file).
- **RLS-first**: every user-facing table needs RLS policies. Picks must NOT be
  visible to other users before match kickoff — that's enforced by RLS, not the
  application layer.
- Scoring writes go through SQL functions, all using `point_awards.idempotency_key`
  to dedupe. Never raw-insert into `point_awards`.

## Known gotchas
- `0003_cron.sql` requires two Postgres GUCs set in the Supabase dashboard before
  apply: `app.cron_app_url` and `app.cron_secret`. Without them, `call_cron_endpoint()`
  silently `raise warning`s.
- Re-applying `0003_cron.sql` first unschedules existing jobs by name (`sync_fixtures`,
  `sync_scorers`) so it's idempotent. If you add a new cron job, follow the same
  pattern — unschedule by name, then re-schedule.
- After applying a migration that changes table schema, regenerate the TS types in
  `lib/supabase/types.ts` via `npm run db:types` and commit them in the same PR.

## Recent changes
<!-- Newest first. Keep last 10. One line per entry. -->
- 2026-07-01: `0035_cron_config_observability.sql` — makes a mis-configured cron **visible in the admin UI**. `call_cron_endpoint()` short-circuits with `raise warning` + `return` when the `app.cron_app_url` / `app.cron_secret` GUCs are missing, but that warning only lands in the Postgres logs — the pg_cron job still reports `succeeded` (it dispatched nothing) and no row is written anywhere the team looks. This hole bit us: after an infra event wiped the GUCs, the `*/10` sync silently did nothing for weeks while `cron.job_run_details` showed green (the timeout in 0034 was moot — no request was even sent). Recreates `call_cron_endpoint()` verbatim from the 0034 body (30 s pg_net timeout retained) with one addition: on the missing-GUC branch it also `insert`s an `external_sync_log` row (`source='pg_cron'`, `endpoint=path`, message explaining the fix) so the admin "Recent sync log" shows the mis-config instead of the cron looking healthy. Append-only — 0003/0034 untouched; `create or replace` is idempotent and the scheduled command picks up the new body (no re-schedule). No schema change (`external_sync_log` exists since 0001) → no `npm run db:types`; no point values touched → points-sync holds. Only covers the GUC-missing case; a dispatched-but-failed pg_net call (timeout/non-200) is still only in `net._http_response`.
- 2026-06-30: `0034_cron_http_timeout.sql` — fixes the `*/10` `sync_fixtures` cron silently never updating data (the team had to hit the admin "Sync fixtures" button every time). `net._http_response` showed every run failing with "Timeout of 5000 ms reached" while DNS (~5 ms) + TCP/SSL handshake (~18 ms) succeeded — i.e. the URL/host/secret were fine, the endpoint just didn't answer within pg_net's **5000 ms default**, because `syncFixtures()` (football-data list + up to 5 detail fetches under a 10-req/min limit + ~104 upserts + scoring RPCs) takes longer than 5 s. The admin button calls `syncFixtures()` in-process (no HTTP, no pg_net timeout), which is why manual always worked. Recreates `call_cron_endpoint()` (the only change vs the 0003 body: `net.http_post(..., timeout_milliseconds := 30000)`); paired with `export const maxDuration = 60` on both cron route handlers so Vercel's low platform default doesn't kill the function before the longer pg_net timeout matters. Append-only — 0003 untouched; the scheduled `select call_cron_endpoint(...)` command is unchanged and picks up the new body, so no re-schedule. No schema change → no `npm run db:types`; no point values touched → points-sync holds.
- 2026-06-30: `0033_close_round2_lock_overrides.sql` — closes every league that had post-knockout bracket access (granted via 0032). Resets `tournament.locked_overrides -> 'round2_open_leagues'` back to `[]` with `jsonb_set(coalesce(locked_overrides, '{}'::jsonb), '{round2_open_leagues}', '[]'::jsonb) where id = 1`, so `round2_locked_for(uid)` falls back to the global `knockout_start_at` lock for everyone — the bracket is locked for all leagues again. Pure data UPDATE: no schema change (no `npm run db:types`), no function/trigger change, no point values touched (points-sync holds); idempotent + NULL-safe (the `coalesce`), append-only (0032 untouched). To reopen a league later, append its id to the array again per 0032's header.
- 2026-06-30: `0032_round2_lock_overrides.sql` — per-league exemption from the round-2 (bracket) lock so the knockout picker can reopen for some leagues while others stay locked. The lock was a single global `tournament.knockout_start_at` with no per-cohort hook; this reuses the dormant `tournament.locked_overrides` jsonb column — leagues whose id is in `locked_overrides -> 'round2_open_leagues'` (a jsonb array of uuid strings) stay editable past the global knockout start, and a user is exempt iff they belong to ≥1 listed league. New `round2_locked_for(uid uuid)` (SECURITY DEFINER, `search_path=public`, like `is_league_member` 0008 — so the `league_members` read isn't RLS-bound under the trigger's end-user role): returns global-lock-passed **AND** the user is **not** in an exempted league. `enforce_round2_lock()` recreated **verbatim from 0030** (privileged bypass + `tg_op='DELETE' → return old` preserved) with one change — the lock test is now `round2_locked_for(coalesce(new.user_id, old.user_id))` instead of `round2_locked()`. `round2_locked()` left defined (unused now, back-compat); the `bracket_predictions` trigger binds by function name so no trigger recreate. Open a league by appending its id to the jsonb array (one-liner in the migration header); revoke by removing it. Mirrored TS-side (`computeLockState(t, now, {round2Exempt})`, `lib/predictions/round2-access.ts`, the bracket page, and the `setBracketPick*` actions) so UI + actions match the trigger. Append-only (0001/0030 untouched), idempotent, schema unchanged (`locked_overrides` already existed) → no `npm run db:types`; no point values touched → points-sync holds.
- 2026-06-16: `0031_global_leaderboard.sql` — adds `get_global_standings()`, a SECURITY DEFINER (authenticated-only) accessor powering the new portal-wide `/leaderboard` page. Sums each onboarded user's **global** point awards (`point_awards.league_id IS NULL` — so the league-scoped crown 👑 / wooden-spoon 💩 bets added in 0023 don't distort the cross-league total) into the same match/bracket/tournament/prop/total breakdown as `league_standings`, returning only users who've scored (`HAVING total > 0`), highest first. Unlike the league board it's a **live aggregation function, not a matview** — no `refresh_*` wiring, no scorer changes — so new awards surface on the realtime refetch. Exposes only aggregate username/display_name/points (no picks) → no RLS pick-leak; `revoke … from public, anon` + `grant … to authenticated` mirrors 0027's `get_league_standings`. Append-only (adds one brand-new function, touches nothing prior migrations own); no table/column change → no `npm run db:types` strictly required (`types.ts` hand-gains the RPC + a `GlobalStandingsRow` alias meanwhile); no point values touched → points-sync holds.
- 2026-06-09: `0026_reveal_group_picks_at_round1_lock.sql` — reveals group-stage 1X2 picks (`match_predictions`) to league-mates once **round 1 locks** (first kickoff) instead of per-match. Drops the per-match `mp_read_after_kickoff` from `0001_init.sql` (gated on each match's own `kickoff_at <= now()`) and recreates it as `mp_read_after_lock`, swapping that EXISTS-over-`matches` clause for `round1_locked()` — i.e. exactly the shape of `tp_read_after_lock` / `pp_read_after_lock`. Round-1 picks are immutable after the lock (lock trigger + `round1_locked()`), so revealing the whole group slate at once leaks no editable info; it just stops a friend's matchday-10 pick hiding until matchday 10. Powers the new Pick-personality cohort (you-vs-league bars + boldness) and the profile "Recent picks" list from group-stage start. `/match/[id]` is unaffected (self-gates its friends-picks fetch on `matchIsLocked(match.kickoff_at)` before RLS); `mp_read_self`/`mp_write_self` and `bracket_predictions` reveal (still per-slot) untouched. RLS-only, append-only (0001 not edited), idempotent (drops both policy names first) — no schema change → no `npm run db:types`, no point values touched.
- 2026-06-09: `0025_relax_total_goals_cap.sql` — removes the `0..300` upper bound on `tournament_predictions.total_goals_guess`, keeping only a `>= 0` floor. WC 2026 has 104 matches, so a high-scoring tournament (~3 goals/match) already clears 300 — the cap was a binding limit on a legitimate guess, and the closest-guess scorer (`score_total_goals_guess`) means an unbounded value can never inflate points (a wild number just loses). Drops the inline `0..300` CHECK added by 0005 (auto-named `tournament_predictions_total_goals_guess_check` — standard `<table>_<column>_check`, the only CHECK on the column) and re-adds `total_goals_guess >= 0` under the same name; drop-then-add keeps it idempotent on re-apply. Append-only (0005 untouched); no column-type change → no `npm run db:types`. Mirrored in `lib/predictions/actions.ts` (`setTotalGoalsGuess` drops its `> 300` branch) and `components/predict/NumberInput.tsx` (`max` now optional). No point-value change → points-sync invariant holds.
- 2026-06-09: `0024_raise_invite_max_uses.sql` — raises the league-invite usage cap from 25 to 100. Friends-league links are shared widely and one link is expected to admit ~60 people, but `createInvite` stamped `max_uses = 25`, so links hit "Invite has reached its limit." too early. `lib/leagues/actions.ts` now writes `max_uses = 100` (named `INVITE_MAX_USES` const) for new invites; this migration `update`s existing rows still on the old 25 cap (`where max_uses = 25`) so already-shared links keep working without being recreated. Pure data UPDATE — non-destructive (raising a cap only admits more people; `redeem_league_invite()` from 0009 still enforces it atomically under a `for update` row lock), idempotent, no schema change → no `npm run db:types`.
- 2026-06-08: `0023_league_internal_bets.sql` — internal league bets (crown 👑 + wooden spoon 💩), settled at group-stage end. **Makes points league-scoped**: adds nullable `point_awards.league_id` (NULL = global, set = league-only) and **recreates `league_standings`** identical to 0002 except the join gains `and (pa.league_id is null or pa.league_id = lm.league_id)` (drop fn → drop matview → recreate matview + indexes → recreate `refresh_league_standings` → seed `refresh` since runtime CONCURRENTLY can't prime an empty matview). New `league_group_bets(league_id, voter_id, bet_kind∈{most_points,least_points}, votee_id)` — RLS `lgb_read_self` / `lgb_read_after_lock` (`round1_locked() and is_league_member`) / `lgb_write_self` (voter + votee both members) + `enforce_round1_lock` trigger. Three constants `points_league_loser_guess()`=5 / `points_league_loser_per_vote()`=2 / `points_league_crown_penalty_per_vote()`=5, mirrored in `lib/scoring/rules.ts` `POINTS.leagueBet`. Reconcile scorer `score_league_group_bets()` (SECURITY DEFINER, `search_path=public,pg_temp`): guards on the whole group stage FINISHED, ranks members by group-stage 1X2 points (`prediction_type='match'` on `stage='GROUP'`; league_bet awards excluded → no feedback loop), settles only leagues with a spread (`max<>min`, ties → multiple winners/losers), delete-stale-then-insert league-scoped awards (`'tournament'` type, `league_bet:` key prefix, negative for crown) via an `on commit drop` temp table. Driven from `settle_group_stage_props()` (recreated to `perform` it after `score_first_eliminated()`) — `syncFixtures()` already calls that after `score_match()` and before `refresh_league_standings()`, so no `sync.ts` change. Append-only (0002/0021 untouched). `types.ts` hand-edited for the new table + `point_awards.league_id` (dev DB not yet applied; `npm run db:types` reproduces once it has).
- 2026-06-08: `0022_manual_admin_props.sql` — seven admin-resolved "house special" props (5 pts each via `points_manual_prop()`, mirrored in `rules.ts` `POINTS.manualProp` + `rules.test.ts`). Adds seven user-pick columns to `tournament_predictions` (`neymar_minutes_pick` / `streaker_pick` bool, `best_goalkeeper_player_id` / `golden_boot_team_id` / `war_game_match_id` uuid FKs `on delete set null`, `own_goals_guess` / `swedish_players_guess` int 0..50) — they inherit 0001's round-1 lock trigger + RLS for free. Admin answers live in a new `manual_prop_resolutions` table (one row per `prop_key`, typed `answer_*` columns, `mpr_read` select-true RLS; writes are service-role only via the admin action). Scoring is a standalone `score_manual_props()` driver of seven reconciling sub-scorers — exact-match `score_neymar_minutes` / `score_streaker` / `score_best_goalkeeper` / `score_golden_boot_team` / `score_war_game` (full 5 pts to every correct picker) and closest-guess ties-split `score_own_goals_guess` / `score_swedish_players_guess` (cloned from `score_total_goals_guess`). It is **not** chained into `score_tournament()` (which early-returns until the Final is FINISHED); the admin `setManualPropResolutions` action calls `score_manual_props()` + `refresh_league_standings` directly, so a prop settles the moment it's resolved (possibly mid-tournament) and re-scores on every edit. Append-only — only `create or replace`s brand-new functions + adds a table/columns, touching nothing 0002–0021 own. Schema change → `types.ts` hand-edited (dev DB hasn't applied; `npm run db:types` reproduces once it has).
- 2026-06-08: `0021_remove_group_winner_prop.sql` — retires the group-winner prop entirely (the pick was redundant with the group-stage 1X2 picks, which already imply each group's winner). Rewrites `settle_group_stage_props()` to drive only `score_first_eliminated()` (owned by 0017); `drop function`s `score_group_winner(char)` + `points_group_winner()`; deletes any `tournament:group_winner:%` awards (none pre-tournament — standings self-heal on the next `syncFixtures()` run); `drop table ... cascade`s `group_winner_predictions` + `group_settlements` (clearing their RLS policies, indexes, lock trigger). Append-only — 0005/0019/0020 untouched; the superseding `create or replace` + drops have no tracked PL/pgSQL dependencies. Pairs with `POINTS.tournament.groupWinner` removed from `lib/scoring/rules.ts` and `types.ts` hand-edited to drop the two table types + `score_group_winner` RPC (dev DB hasn't applied yet — `npm run db:types` reproduces once it has). Points-sync invariant holds (no surviving twin to mismatch).
- 2026-06-08: `0020_more_outright_props.sql` — four new tournament-wide closest-guess props on `tournament_predictions` (`final_goals_guess` 0..30, `biggest_win_margin_guess` 0..30, `golden_boot_goals_guess` 0..30, `total_red_cards_guess` 0..200), each with a `points_*_base()` constant (10/10/10/15, mirrored in `lib/scoring/rules.ts` `POINTS.tournament`) and a `score_*_guess()` scorer cloned from `score_total_goals_guess()` (single closest wins, ties split, reconcile-then-insert with the points-aware stale predicate). `score_final_goals_guess` reads the Final's scoreline; `score_biggest_win_margin_guess` reads `max |home-away|` over FINISHED matches; `score_golden_boot_goals_guess` (max goals/player in `player_goal_log`) and `score_total_red_cards_guess` (RED+YELLOW_RED in `player_card_log`) are **drain-gated** on `all_match_details_synced()` like top-scorer/troublemaker. All four guard on the Final being FINISHED. `score_tournament()` re-created from its 0016 body with the four sub-scorers appended — so **0020 now owns `score_tournament()`** (base future redefinitions on this version). Schema change → `types.ts` hand-edited (the dev DB hasn't applied it; `npm run db:types` reproduces the same once it has). Powers the new `/predict/outcomes` tab.
- 2026-06-05: `0019_reconcile_group_stage_props.sql` — brings `score_group_winner(group)` under 0014's reconcile model. It was one-shot: early-returned on the `group_settlements` write-once latch, so a group result corrected post-settlement (admin override or football-data revision) could never move the 5-pt group-winner award. Now drops the early-return, re-derives the latch from the current result via `on conflict (...) do update ... where winner_team_id is distinct from excluded.winner_team_id` (no-op when unchanged), DELETEs the awards it owns whose prediction no longer matches the current winner (stale-only predicate), then re-INSERTs `on conflict do nothing`; if the group is no longer fully decided (a FINISHED match reverted) it deletes every owned award and clears the latch. `settle_group_stage_props()` drops its `not exists (group_settlements ...)` filter and calls `score_group_winner(g)` for every group every run (safe — idempotent on unchanged results); it still `perform`s `score_first_eliminated()` each run. `score_first_eliminated()`'s matching reconcile + 48-team detection fix lives in `0017_fix_first_eliminated_48team.sql` (#81) — this migration no longer redefines it, so the two PRs can't clobber each other. Point value unchanged (`points_group_winner()` = 5), so points-sync with `rules.ts` holds. No schema change → no `npm run db:types`. pgTAP coverage deferred to #70. Closes #82.
- 2026-06-05: `0017_fix_first_eliminated_48team.sql` — rewrites `score_first_eliminated()` for WC 2026's 48-team format AND brings it under 0014's reconcile model. The 0005 version flagged a team the moment it was out of its group's top 2 (correct for the old 32-team format, wrong now that the 8 best third-placed teams also advance), so the 10-pt prop could settle on a team that ultimately qualifies as a best-third and — being one-shot via the `first_elimination` row, and skipped by 0014's reconcile — never self-correct. New rule flags a team only when mathematically out of BOTH group top-2 AND the best-8-thirds race: `rivals_above >= 3` (4th-or-worse), OR `rivals_above >= 2 AND groups_third_above >= 8` where `groups_third_above` counts other groups whose 3rd-place points floor (3rd-largest current pts, a guaranteed lower bound) strictly exceeds the team's max reachable points. Sound/conservative (never wrong, may settle late) — strict point inequalities, no GD/GF tiebreaks. Reconcile path: drops the one-shot early-return, re-derives the latch (`on conflict do update ... is distinct from`), deletes stale awards, clears award+latch when no team is eliminated. This migration OWNS `score_first_eliminated()`; the companion `0019_reconcile_group_stage_props.sql` (#82) no longer redefines it (only `score_group_winner` + `settle_group_stage_props`), so the two PRs can't clobber each other. Function body only — no schema change, no `db:types`, point value stays 10 (points-sync holds); `0005`/`0014` untouched. Canonical TS mirror + tests in `lib/scoring/first-eliminated.ts` / `.test.ts`. Closes #81; Refs #82.
- 2026-06-05: `0018_scoring_hygiene.sql` — scoring SQL hygiene, no point-value change. (1) `drop function if exists points_dark_horse()` — the flat-10 function was dead since 0005 made dark-horse rank-based; the only references live in the superseded `score_tournament()` bodies in 0002/0004 (PL/pgSQL bodies create no tracked function dependencies, so the drop is safe), and the live `score_tournament()` (0014) pays `teams.fifa_ranking`. It's absent from `lib/scoring/rules.ts` so points-sync is unaffected. (2) `score_bracket()` re-created identical to 0014 but with `bp.bracket_slot <> '3RD'` added to the keep-set in both the reconcile DELETE and the per-slot INSERT — a stray `bracket_slot='3RD'` prediction (only reachable via direct DB writes; the UI's `R32→R16→QF→SF→F→W` never offers it) previously minted a 0-point `bracket` award because `points_bracket_slot('3RD')` falls through to `else 0`. Now no new 3RD award is written and any pre-existing one is reaped on the next run. Totals unchanged.
- 2026-06-05: `0016_gate_tournament_drain.sql` — defers the two drain-dependent tournament categories so they never score on incomplete data right after the Final (#83). New SECURITY DEFINER helper `all_match_details_synced()` returns true once no FINISHED match has `details_synced_at IS NULL`. `score_troublemaker()` returns 0 early (after its existing Final-finished guard) until that's true; `score_tournament()` wraps only its top-scorer reconcile+award block in `if all_match_details_synced()`. Other categories (winner / runner-up / dark horse / player props / total goals / highest match) unchanged and still award immediately. Self-healing: `score_tournament()` re-runs every 10-min cron while the Final is FINISHED, so the gate opens once the per-match detail drain catches up (also accelerated by the repurposed `syncScorers()` daily backstop). `create or replace` only — no schema change, so no `npm run db:types` needed. Point values unchanged.
- 2026-06-04: `0015_profile_onboarding.sql` — adds `profiles.onboarded boolean not null default false`, driving the new `/welcome` username-picker gate in `app/(app)/layout.tsx`. `default false` applies to existing rows too, so every current account is prompted once on next login — intentional, since it retro-fixes already-joined members who only have an auto-generated handle. `handle_new_user()` is deliberately unchanged: its INSERT omits the column, so new rows inherit the `false` default. No RLS change (`profiles_update_self` from 0001 already covers the user's self-update). Types were hand-edited (`onboarded` on `profiles` Row/Insert/Update) because the dev DB hasn't applied the migration; `npm run db:types` reproduces the same once it has.
- 2026-06-01: `0014_reconcile_scoring.sql` — scorers now **reconcile** (delete-stale-then-insert) instead of insert-only, fixing permanently-inflated standings after a result correction. `point_awards.idempotency_key` encodes only (user, target), never the result, so `on conflict do nothing` could never revoke a now-wrong award when an admin override or a football-data revision flipped a winner. `score_match` / `score_bracket` / `score_tournament` (+ its sub-scorers `score_total_goals_guess` / `score_highest_match_goals_guess` / `score_troublemaker`) each first DELETE the awards they own that no longer match the current result, then re-INSERT the correct set. The delete predicate is stale-only (compares membership, plus `points` for the rank/split-derived awards: dark horse, total-goals, highest-match) so an unchanged run removes zero rows and stays idempotent under the 10-min cron. Point values unchanged — points-sync with `rules.ts` holds. **Known limitation** (resolved by `0019_reconcile_group_stage_props.sql` for group-winner and `0017_fix_first_eliminated_48team.sql` for first-eliminated): one-shot group-stage settlements (`score_group_winner` / `score_first_eliminated`, gated by `group_settlements` / `first_elimination` rows) didn't re-settle if a group result was corrected post-settlement.
- 2026-05-26: `0013_add_r32_stage.sql` — adds `R32` to the `stage` enum (positioned BEFORE `R16` for natural ordinal sort) and extends `points_bracket_slot()` so `R32-%` slots are worth 1 pt. WC 2026's expanded format makes Round of 32 the first knockout round (12 group winners + 12 runners-up + 8 best 3rd-place = 32 teams). `score_bracket()` is unchanged — it joins on `bracket_slot` text so new R32-1..R32-16 slots score transparently once matches land. UI / auto-advancement / per-stage filtering arrives in follow-up PRs.
- 2026-05-25: `0011_banter.sql` — adds `banter_messages` + `banter_replies` (FK cascade on parent delete) for league chat. RLS gated by the existing `is_league_member(uuid, uuid)` helper plus a new `banter_message_league_id(uuid)` SECURITY DEFINER companion so the reply policies can resolve the parent's league_id without recursing through RLS on `banter_messages`. 1..180 char body CHECK constraint; indices on `(league_id, created_at desc)` and `(message_id, created_at asc)`. Both tables registered with the `supabase_realtime` publication (idempotent via the `duplicate_object` swallow) so the `league:<id>:banter` channel receives INSERT/DELETE events.
- 2026-05-25: `0012_pick_reactions.sql` — polymorphic emoji reactions on picks. New table `pick_reactions(id, pick_id text, pick_kind text, user_id, emoji text, created_at)` with CHECK on `pick_kind ∈ {match, bracket, tournament, prop}` and `emoji ∈ {🔥, 💩, 😱, 👍}`, plus `unique (pick_id, pick_kind, user_id, emoji)` for toggle semantics. Two SELECT policies (UNIONed): `pr_read_self` and `pr_read_league_mate` (inline EXISTS over `league_members` joined to itself — mirrors `mp_read_after_kickoff`, no recursion concern); `pr_write_self` for all writes. `pick_id` is text (not uuid) because targets are polymorphic across four prediction tables; v1 UI wires only the `match` kind. No FK on `pick_id` — orphan cleanup is a future sweeper. Closes #36 data layer.
- 2026-05-24: `0010_backfill_team_group_letter.sql` — adds `backfill_team_group_letters()` SECURITY DEFINER RPC that derives `teams.group_letter` from group-stage `matches.group_letter` (distinct-on per team, won't override if already correct). `seedTeams()` never wrote `group_letter` (football-data's /teams payload has no group info) so the column was permanently NULL, yet `GroupWinnerPicker` and `score_first_eliminated` both read it. Now invoked from `syncFixtures()` after the match upsert loop. Also serves as the recovery path for the parser bug fixed in the same PR — previously `m.group.replace("Group ", "").slice(0, 1)` returned "G" for every group because the v4 API format is `GROUP_A`, not `Group A`.
- 2026-05-24: `0009_redeem_league_invite_citext_cast.sql` — Sentry caught `42804: structure of query does not match function result type` once migration 0004 was applied to prod: the function declared `league_slug text` but `leagues.slug` is `citext`, so the success-path `select true, inv.slug, null::text` mismatched column 2. Casts the slug to `text` in the success branch (`inv.slug::text`). Signature unchanged; TS callers untouched.
- 2026-05-22: `0008_rls_security_definer_helper.sql` — Sentry confirmed the OR'd `user_id = auth.uid()` bootstrap from 0006 didn't actually break the recursion. Postgres evaluates the OR'd EXISTS subquery regardless of short-circuiting and raises `infinite recursion detected in policy for relation "league_members"`. New `is_league_member(league_id, user_id)` SECURITY DEFINER function bypasses RLS for the membership check; `league_members_read_self_leagues` and `leagues_read_members` now both route through it instead of EXISTS'ing back into `league_members`.
- 2026-05-22: `0007_leagues_owner_base_case.sql` — 0006 wasn't enough; users still hit "League created but not visible to you" on prod. Adds an `owner_id = auth.uid()` base case to `leagues_read_members` so a creator can read their league back regardless of how the `league_members` EXISTS subquery resolves. Also adds a `debug_auth_uid()` SQL function used by the createLeague action to capture the PostgREST-side `auth.uid()` into Sentry when the visibility check still fails.
- 2026-05-22: `0006_fix_league_members_rls.sql` — adds a `user_id = auth.uid()` bootstrap clause to `league_members_read_self_leagues` so the policy isn't fully self-referential. Without it, the EXISTS subquery hit Postgres' RLS recursion-breaker on freshly-created leagues, hiding the creator's own membership row and (via the `leagues_read_members` EXISTS check) the league itself — the root cause of the post-create 404.
- 2026-05-22: `0005_more_tournament_props.sql` — five new tournament-wide props (total goals, highest-match goals, troublemaker, group winners ×12, first eliminated); rewrites dark-horse scoring as rank-based (`teams.fifa_ranking`); adds `matches.details_synced_at` marker; extends `score_tournament` to chain into the new scorers.
